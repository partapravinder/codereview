package com.newgen.service;

//import com.netflix.appinfo.InstanceInfo;
//import com.netflix.discovery.EurekaClient;
//import com.netflix.discovery.shared.Application;

//@Service
public class EurekaUrlResolver {
	/*
	 * 
	 * //@Autowired private EurekaClient eurekaClient;
	 * 
	 * private String metadataCoreServiceUrl;
	 * 
	 * public String procureUrl(String serviceName) { //Get Metadata service url
	 * from Eureka Server Application application =
	 * eurekaClient.getApplication(serviceName); InstanceInfo instanceInfo =
	 * application.getInstances().get(0); metadataCoreServiceUrl =
	 * "http://"+instanceInfo.getIPAddr()+ ":"+instanceInfo.getPort(); return
	 * metadataCoreServiceUrl; }
	 */}
