package com.newgen.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MultiFilePartConfig {

	/*
	 * @Value("${content.maxUploadSize}") String content_maxUploadSize;
	 * 
	 * @Bean public MultipartConfigElement multipartConfigElement() {
	 * MultipartConfigFactory factory = new MultipartConfigFactory();
	 * factory.setMaxFileSize(content_maxUploadSize);
	 * factory.setMaxRequestSize(content_maxUploadSize); return
	 * factory.createMultipartConfig(); }
	 */

}
